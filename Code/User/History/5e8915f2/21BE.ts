function sum(x: number, y: number) {
    return x + y
}

function mul(x: number, y: number) {
    return x * y
}

function div(x: number, y: number) {
    return x / y
}

export default {
    sum: sum,
    mul: mul,
    div: div,
}
